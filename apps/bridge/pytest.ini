[pytest]
testpaths = tests
markers =
    unit: fast tests with mocked CARLA
    integration: requires real CARLA running
    e2e: full end-to-end smoke test
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
